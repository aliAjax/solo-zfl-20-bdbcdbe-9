{
  "name": "rubbing-repair-api",
  "version": "1.0.0",
  "private": true,
  "description": "古籍拓片缺损修补与影像入库流水线（零依赖 Node 服务）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-timeout=30000 test/"
  },
  "engines": {
    "node": ">=20"
  }
}
